<Window x:Class="ElectronicShopBusarev.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectronicShopBusarev"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="АВТОРИЗАЦИЯ" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Логин"/>
            <TextBox x:Name="Login"/>

            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="Password"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="2">
            <Button Content="Вход" Margin="5" Click="OpenMenuWindow"/>
            <Button Content="Регистранция" Margin="5" Click="OpenRegistrationWindow"/>
        </StackPanel>
    </Grid>
</Window>
